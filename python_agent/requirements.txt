# GeologyGPT v4 Python Agent — Fixed Requirements
# Install: pip install -r requirements.txt

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Environment / config ──────────────────────────────────────
python-dotenv==1.0.1

# ── LangGraph agent (standalone — no LangChain needed) ───────
langgraph==0.1.14

# ── AI / Embeddings ───────────────────────────────────────────
# Gemini via direct REST (httpx) — no SDK needed
httpx==0.27.0
cohere==5.5.8          # v5 uses ClientV2, not Client

# ── Vector DB ────────────────────────────────────────────────
# IMPORTANT: package is now "pinecone", NOT "pinecone-client"
pinecone==3.2.2

# ── Web search ────────────────────────────────────────────────
tavily-python==0.3.3

# ── Async fix — needed for asyncio inside FastAPI ────────────
nest-asyncio==1.6.0

# ── Utilities ─────────────────────────────────────────────────
pydantic==2.7.4
